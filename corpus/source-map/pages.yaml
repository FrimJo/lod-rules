# Mapping between physical PDF pages and printed page labels. Populated in Phase 1.
# The two numbering systems are not assumed to be identical.
# Schema: schemas/source-map.schema.json (pages root)
[]
