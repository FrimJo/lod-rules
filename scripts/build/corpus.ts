// Phase 11 builds generated/ artifacts from the canonical corpus.
// The command exists now so the interface is stable, but there is nothing to compile yet.
console.error('build:corpus is not implemented yet (Phase 11).');
process.exit(1);
